services:
  app:
    build: .
    image: cpa-exam-app
    ports:
      - "3020:3000"
    volumes:
      - db-data:/app/prisma/data
    environment:
      - DATABASE_URL=file:/app/prisma/data/cpa-exam.db

volumes:
  db-data:
